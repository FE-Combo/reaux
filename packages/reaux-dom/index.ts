import {start, register, PModel, GModel} from "./src/boot";
import Route from "./src/Route";
import {handlerDecorator, Loading, Lifecycle} from "./src/decorator";

export {start, register, PModel, GModel, Route, handlerDecorator, Loading, Lifecycle};
